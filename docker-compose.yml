version: '3.8'

services:
  web:
    build: .
    container_name: devops_lw1-web-1
    ports:
      - "8000:8000"
    volumes:
      - ./experiments:/app/experiments
    environment:
      - PYTHONUNBUFFERED=1